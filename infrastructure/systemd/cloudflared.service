[Unit]
Description=Cloudflare Tunnel for Admin Dashboard
After=network.target

[Service]
Type=simple
User=%i
ExecStart=/usr/local/bin/cloudflared tunnel run admin-dashboard
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
